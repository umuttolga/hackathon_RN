import React from "react";
import { Text, View } from "react-native";

const AppEntry = () => {
  return (
    <View className="m-auto">
      <Text className="text-[32px] text-center">
        Brainstorm App (Hope to change later)
      </Text>
    </View>
  );
};

export default AppEntry;
